{% extends 'base.html.twig' %}
{% block body %}
    
<article>
    <h2>{{ article.title }}</h2>
    <div class="metadata">Ecrit le 10/10/19 dans la catégorie {{article.category.title}} </div>
    <div class="content">
        <img src="{{ article.image }}" alt="">
        <p>{{article.content}}</p>
    </div>
    <a href="{{ path('article_edit', {'id': article.id}) }}" class="btn btn-secondary">Modifier l'article</a>
    <a href="{{ path('article_delete', {'id': article.id}) }}" class="btn btn-danger">Supprimer l'article</a>
</article>

{% endblock %}